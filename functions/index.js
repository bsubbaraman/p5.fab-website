// No Cloud Functions are currently deployed.
//
// Username claiming used to live here (claimUsername). It is now enforced entirely by
// Firestore security rules — see the usernames/{key} collection in firestore.rules.
// The previously-deployed function was removed with:
//   firebase functions:delete claimUsername
